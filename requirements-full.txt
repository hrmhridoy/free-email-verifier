# Core dependencies (used by all interfaces)
dnspython==2.4.2
requests==2.31.0

# For Streamlit interface (app.py)
streamlit==1.28.1

# For Flask interface (flask_app.py)
Flask==2.3.3

# Optional but recommended
python-dotenv==1.0.0  # For environment variable management
